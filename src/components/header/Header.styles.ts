import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      display: "flex",
      height: 90,
      width: "100%",
      backgroundColor: "#3C96C9",
      justifyContent: "center"
    },
    title: {
      fontSize: 25,
      fontWeight: "bold",
      marginLeft: 20,
      color: "white"
    }
});